{
  "version": 2,
  "buildCommand": "cd frontend && npm install && npm run build && cp ../public/pdf.worker.js dist/",
  "outputDirectory": "frontend/dist",
  "functions": {
    "api/upload.js": {
      "maxDuration": 30
    },
    "api/ocr.js": {
      "maxDuration": 60
    }
  }
}